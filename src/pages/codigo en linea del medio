export default function TeamPage() {
  return (
    <div className="min-h-screen bg-gray-50 py-16">
      <h1 className="text-4xl font-bold text-center mb-16">Nuestro Equipo</h1>

      <div className="relative max-w-6xl mx-auto">
        {/* Línea vertical */}
        <div className="absolute left-1/2 transform -translate-x-1/2 h-full w-1 bg-gray-300"></div>

        <div className="flex flex-col space-y-12">
          {team.map((member, index) => {
            const isLeft = index % 2 === 0;
            return (
              <div key={index} className={`flex ${isLeft ? "justify-start" : "justify-end"} w-full`}>
                <div
                  data-aos={member.aos || "fade-up"}
                  className="relative bg-white rounded-2xl shadow-md p-6 text-center hover:shadow-xl transition w-full max-w-xl"
                >
                  <img
                    src={member.photo}
                    alt={member.name}
                    className="w-24 h-24 rounded-full mx-auto mb-4 object-cover"
                  />
                  <h2 className="text-xl font-semibold">{member.name}</h2>
                  <p className="text-gray-500">{member.role}</p>
                  <p className="mt-2 text-sm text-gray-600">{member.bio}</p>

                  {/* Punto en la línea */}
                  <span className={`absolute top-1/2 transform -translate-y-1/2 w-4 h-4 bg-blue-500 rounded-full ${isLeft ? "right-[-30px]" : "left-[-30px]"}`}></span>
                </div>
              </div>
            );
          })}
        </div>
      </div>
    </div>
  );
}
